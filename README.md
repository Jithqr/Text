My game's UI isn't adapting properly to different screen sizes — elements are sized for one specific viewport and don't scale/reflow correctly on smaller or larger screens (e.g. laptop screens vs desktop monitors). Please do a full responsive layout pass across the entire app:
Audit all screens (Home, Wild Hunt, Wild Battle, Player Stats, Pokédex, Caught Pokémon, Trainer Card, Settings, etc.) for fixed pixel widths/heights on containers, images, and cards. Replace these with responsive units (%, vw/vh, max-width, aspect-ratio, flex, grid) so layouts scale fluidly instead of breaking or overflowing.
Scene/encounter images (the forest battle background, Pokémon sprites) should sit in a container with a fixed aspect-ratio and object-fit: cover, so they scale proportionally at any screen width instead of stretching, cropping oddly, or leaving gaps.
Add proper responsive breakpoints using Tailwind's responsive prefixes (sm:, md:, lg:, xl:) so layouts adjust cleanly across mobile, tablet, laptop, and large desktop/monitor sizes — test especially at common widths like 375px, 768px, 1024px, 1440px, and 1920px+.
Check the root layout/viewport setup — make sure there's a proper viewport meta tag, and that no top-level container has a hardcoded width/height that prevents the page from filling or adapting to the actual browser window.
Text and buttons should also scale sensibly (avoid fixed font-sizes that look tiny or huge depending on screen size — use relative units or Tailwind's responsive text classes).
Please go through each page/component systematically, fix the responsive issues, and test resizing the browser window (and if possible, different device widths) to confirm everything adapts smoothly without overflow, cropping, or misalignment.
